Home-Manager not being used: This is because it's using the one in nixos...
  apps/* in nixpkgs not being used.
  sudo needed to see home-manager and pkgs channels.

Bash aliases not working: Resolved by adding nixpkgs data to nixos. Not ideal.
  Related to the above issue.

Make VScode auto-trust: RESOLVED

Remove bezels and make top-bar autohide:
  hide-top-bar isn't working cos it's outdated. 

Python Tabbing:
  Turns out Vim fucks with Python... nicee
    Had further problems with this, but have been resolved by adding an alias for vim :)

Gitter:
  Bash function for git stuff:
    If repo not initted: git init -> branch etc. -> first commit
    If repo initted: git commit "comment"
    Flags for branching, status, etc. (anything I want really)
  Probably gonna be more than a function, may warrant its own file :)

